# Conflict Genesis - Railway Deployment
# https://railway.app

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn web_app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 2"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
